角色配图说明
============

本目录为 24 位角色的 WebP 立绘（与站点色板统一的国风仙侠 × 二次元风格）：

- 构建命令 `npm run build` 前若存在 `public/characters/*.png`，`prebuild` 会限宽转 WebP；
- 每角色对应 `characters.json` 中的 `heroImage`：`/characters/<id>.webp`。
